import React from "react";

const Picker = () => {
    return (
        <div className="pickerContainer">
            <div className="movieFinder">?</div>
            <button className="movieButton">Dado</button>
        </div>
    );
};

export default Picker;
